<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="resources/css/estilos.css">
  <title>Inicio</title>
</head>

<body>
  <div class="formulario">
    <h1>Inicio</h1>
    <form action="auth" method="post">
      <div class="email">
        <% if (login) { %>
          <p>El usuario conectado es:
            <%= name %>
              <p>
              <div class="registrarse">
                <a href="logout" class="registrarse">Logout</a>
              </div>
              <%} else { %>
                <p>
                  <%= name %>
                </p>
                <div class="registrarse">
                  <a href="login" class="registrarse">Login</a>
                </div>
                <% } %>
      </div>
    </form>
  </div>
</body>

</html>